import React from 'react'

const Cart = () => {
    return (
        <div>
            
        </div>
    )
}

export default Cart

const addToCart = (product) => {
    setChart([...cart, product]);
}